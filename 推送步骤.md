# 🚀 GitHub推送步骤

## ✅ 准备工作已完成

- ✅ Git仓库已初始化
- ✅ Git用户信息已配置（singer2622525-netizen）
- ✅ .gitignore文件已创建
- ✅ 项目文件已准备就绪

---

## 📤 推送步骤

### 步骤1：添加文件并提交

```bash
# 切换到项目目录
cd "/Users/a1/Documents/Projects/cursor/00-00软件工程事业部（AI 数字人队伍）"

# 添加所有文件
git add .

# 提交（使用以下命令之一）
git commit -m "初始提交：软件工程事业部AI数字人队伍项目

- 完成组织架构设计
- 实现9个数字人角色
- 完成智能产品规划师核心功能
- 完成Web界面基础功能
- 完成系统布局和逻辑关系设计"
```

### 步骤2：在GitHub创建仓库

1. 登录GitHub: https://github.com
2. 点击右上角 **"+"** → **"New repository"**
3. 填写仓库信息：
   - **Repository name**: `ai-digital-human-team` （或你喜欢的名字）
   - **Description**: `软件工程事业部 - AI数字人队伍管理平台`
   - **Visibility**: 选择 Public 或 Private
   - ⚠️ **不要**勾选 "Initialize this repository with a README"
   - ⚠️ **不要**添加 .gitignore 或 license（本地已有）
4. 点击 **"Create repository"**

### 步骤3：连接远程仓库并推送

```bash
# 添加远程仓库（替换YOUR_USERNAME和REPO_NAME）
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 例如，如果你的GitHub用户名是 singer2622525-netizen，仓库名是 ai-digital-human-team：
git remote add origin https://github.com/singer2622525-netizen/ai-digital-human-team.git

# 设置主分支为main（如果GitHub使用main作为默认分支）
git branch -M main

# 推送代码
git push -u origin main
```

---

## 🔐 认证方式

### 方式1：使用Personal Access Token（HTTPS）

1. GitHub设置 → Developer settings → Personal access tokens → Tokens (classic)
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成token并复制
5. 推送时，用户名输入你的GitHub用户名，密码输入token

### 方式2：使用SSH（推荐）

```bash
# 检查是否已有SSH密钥
ls -al ~/.ssh

# 如果没有，生成SSH密钥
ssh-keygen -t ed25519 -C "singer2622525@gmail.com"

# 复制公钥
cat ~/.ssh/id_ed25519.pub

# 添加到GitHub: Settings → SSH and GPG keys → New SSH key
# 然后使用SSH URL：
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git
```

---

## ⚡ 一键推送脚本

你也可以使用我创建的脚本：

```bash
./快速推送命令.sh
```

---

## 📋 完整命令示例

假设你的GitHub用户名是 `singer2622525-netizen`，仓库名是 `ai-digital-human-team`：

```bash
# 1. 添加文件
git add .

# 2. 提交
git commit -m "初始提交：软件工程事业部AI数字人队伍项目"

# 3. 添加远程仓库
git remote add origin https://github.com/singer2622525-netizen/ai-digital-human-team.git

# 4. 设置主分支
git branch -M main

# 5. 推送
git push -u origin main
```

---

## ✅ 推送成功后

推送成功后，你应该能看到：
- GitHub仓库页面显示所有文件
- README.md正确显示
- 项目结构清晰

访问你的仓库：
```
https://github.com/YOUR_USERNAME/REPO_NAME
```

---

## 🆘 常见问题

### 问题1：认证失败
- 检查用户名和密码/token是否正确
- 如果使用HTTPS，确保使用Personal Access Token而不是GitHub密码

### 问题2：分支名称不匹配
- 如果GitHub使用 `main`，使用 `git branch -M main`
- 如果GitHub使用 `master`，使用 `git push -u origin master`

### 问题3：远程仓库已存在
```bash
# 删除现有远程仓库
git remote remove origin

# 重新添加
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

---

## 📞 需要帮助？

如果遇到问题，告诉我具体的错误信息，我会帮你解决！
