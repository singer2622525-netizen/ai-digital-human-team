# 密码数据库说明

## 📋 存储位置

**密码数据库存储在项目内，与项目捆绑在一起**

```
项目根目录/
└── 03-技术实现/
    └── 05-配置管理/
        ├── credentials_manager.py      # 密码管理工具
        ├── credentials_api.py          # API接口
        ├── credentials_auto.py          # 自动收集和调用
        ├── credentials_collector.py     # 自动收集器
        └── 密码数据库/
            └── credentials.db           # SQLite数据库（加密存储）
```

## ✅ 设计优势

1. **与项目捆绑**
   - 数据库文件在项目目录内
   - 项目迁移时数据库一起迁移
   - 不会丢失或分离

2. **本地存储**
   - 数据完全存储在Mac本地
   - 不依赖外部服务
   - 安全可控

3. **轻量级**
   - SQLite无需单独部署
   - 无需服务器
   - 开箱即用

4. **加密保护**
   - 所有密码加密存储
   - 使用主密码保护
   - 文件权限控制（600）

## 🔒 安全说明

### 数据库文件保护

```bash
# 数据库文件权限设置为600（只有所有者可读写）
chmod 600 03-技术实现/05-配置管理/密码数据库/credentials.db
```

### Git忽略

数据库文件已在 `.gitignore` 中，不会被提交到Git：

```gitignore
# 密码数据库
03-技术实现/05-配置管理/密码数据库/*.db
```

## 📝 使用方式

### 添加凭证

```bash
cd "03-技术实现/05-配置管理"
python credentials_manager.py add-service RAGFlow --type 知识库 --url https://ragflow.suuntoyun.com
python credentials_manager.py add-credential RAGFlow --type username_password --key username --value your-username
python credentials_manager.py add-credential RAGFlow --type username_password --key password --value your-password
```

### 在代码中使用

```python
from credentials_api import CredentialsAPI

api = CredentialsAPI()
credentials = api.get_credentials("RAGFlow")
```

## 🔄 备份和迁移

### 备份

```bash
# 备份数据库文件
cd "03-技术实现/05-配置管理/密码数据库"
cp credentials.db credentials.db.backup
```

### 迁移

项目迁移时，整个 `03-技术实现/05-配置管理/密码数据库/` 目录会一起迁移。

## 📊 与RAGFlow知识库的区别

| 特性 | 密码数据库 | RAGFlow知识库 |
|------|-----------|--------------|
| 存储位置 | Mac本地（项目内） | Dell服务器 |
| 数据类型 | 账号密码等敏感信息 | 项目文档、经验等知识 |
| 访问方式 | SQLite API | RAGFlow API |
| 加密方式 | Fernet对称加密 | RAGFlow内置加密 |
| 用途 | 凭证管理 | 知识管理 |

**两者互补，各司其职**：
- 密码数据库：管理账号密码（本地、安全）
- RAGFlow知识库：存储项目知识（远程、共享）

---

**密码数据库已与项目捆绑，无需额外部署！**
